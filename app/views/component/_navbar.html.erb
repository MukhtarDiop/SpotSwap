<nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm mb-5">
  <div class="container">
    <!-- Brand -->
    <%= link_to root_path, class: "navbar-brand d-flex align-items-center" do %>
      <img src="https://res.cloudinary.com/dg5qvbxjp/image/upload/v1748375631/ChatGPT_Image_May_27_2025_at_02_53_12_PM_iz8dcr.png" 
      alt="Spotly Logo" 
      class="navbar-logo" />
    <% end %>

    <!-- Bouton hamburger mobile -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Liens de navigation -->
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav mx-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to "Browse Spots", spots_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "List Your Spot", new_spot_path, class: "nav-link" %>
        </li>
      </ul>

      <div class="d-flex">
          <% if user_signed_in? %>
        <!-- Affiche l'email (ou prénom) pour info -->
        <%= link_to profile_path, class: "btn btn-primary" do %>
            <i class="fas fa-user"></i> Profile
        <% end %>
        <!-- Lien Déconnexion : appelle Devise#destroy via DELETE -->
        <%= link_to "Log out", destroy_user_session_path, data: { turbo_method: :delete },class: "btn logout" %>
      <% else %>
        <!-- Lien Connexion -->
        <%= link_to "Login", new_user_session_path,
                    class: "btn btn-primary me-2" %>

        <!-- Lien Inscription (si tu as activé Registrations) -->
        <%= link_to "Sign up", new_user_registration_path,
                  class: "btn btn-success" %>
      <% end %>
      </div>
    </div>
  </div>
</nav>
